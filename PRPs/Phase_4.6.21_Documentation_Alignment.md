# Phase 4.6.21 實作計畫 (Implementation Plan) - Documentation Alignment & Paradox Resolution

> **目標 (Goal)**: 
> 1. 深度稽核 Phase 4.7~4.11 的「文件 vs 代碼」矛盾點，並逐條說明。
> 2. 物理驗證全系統型別債務，確保重構後未回歸。
> 3. 對齊 `CONTRIBUTING_tw.md` 與 `GEMINI.md` 以反映 2026-03-26 的真實硬化現狀。

## 1. Phase 4.7~4.11 矛盾深度稽核 (The Hardening Audit)

本章節列出 `CONTRIBUTING_tw.md` (附錄 C) 與實體代碼之間的具體斷層，作為 4.6.21 的硬化基準。

| 階段 | 文件描述 (宣稱已完成) | 代碼實體現狀 (Mar 26) | 矛盾說明與影響 |
| :--- | :--- | :--- | :--- |
| **4.7** | **神經連結 (MCP)**：系統已全面導入 MCP 並具備自癒能力。 | `mcp_server.py` 在 4.6.20 才剛完成瘦身與穩定化修復。 | **過度樂觀**：2 月份的「導入」僅為初步框架，3 月份發現效能與崩潰問題，實質上的「硬化」是在 4.6.20 才真正達成。 |
| **4.8** | **Agent 覺醒**：移除所有 Mock 邏輯，MarketBot 等已通用的 MCP 運作。 | `agent_service.py` 仍存在 `if command:` 模擬分支，且受限於 **Poisson Gate (XP)**。 | **邏輯殘留**：文件宣稱「已覺醒」，但代碼中仍有 4.6.15 引入的 XP 限制，導致新 Agent 實際上無法調用工具。 |
| **4.9** | **安全與自治**：Clockwork 實現自動巡邏與 Log 分析。 | `scheduler_service.py` 的 Job 觸發仍依賴靜態配置，自治邏輯尚不具備自適應性。 | **功能虛浮**：巡邏邏輯已存在，但「主動派工」給 DevBot 的閉環尚未經過物理壓力測試驗證。 |
| **4.10** | **穩定化**：Indentation 與 NameError 全數修復。 | `mcp_server.py` 在今日 (4.6.20) 才修復了 read-only 屬性的 runtime crash。 | **修復回歸**：4.10 的修復在 March 的大規模檔案拆分中可能發生了部分回歸，需要重新掃描。 |
| **4.11** | **型別清掃**：Zero MyPy Errors 達成。 | 目前針對 `api_routes` 的抽樣 MyPy 通過，但全系統掃描尚未執行。 | **數據斷層**：2 月的 0 錯誤數據是基於舊結構，檔案拆分後（如 `projects_api.py` 拆分）的型別完整性需要今日重新證實。 |

## 2. 執行步驟 (Proposed Changes)

### 2.1 對齊專案食譜 (`CONTRIBUTING_tw.md`)
- [x] **狀態校正**：將 4.7~4.11 重新標記為「整合與硬化現狀」。
- [x] **注入硬化紀錄**：將 4.6.17~4.6.20 的物理修復成果補入附錄 C。
- [ ] **更新原則**：加入「物理穿透驗證」作為防禦幽靈開發的核心心法。

### 2.2 同步廚師日誌 (`GEMINI.md`)
- [ ] **同步日誌**：將 Git Log 中的 4.6.17~4.6.20 結案摘要補入第三章。
- [ ] **修正目標**：確立今日目標為 4.6.21 對齊驗證。

### 2.3 物理基準驗證 (Verification)
- [ ] **MyPy 全量掃描**：執行 `uv run mypy src/server` 並記錄結果。
- [ ] **測試覆蓋確認**：物理證實 `make test-be` 之測試總數為 554 且 100% 通過（需在隔離環境或使用 FORCE 指令）。

## 3. 預期結果 (Expected Outcome)
- 文件不再具有「欺騙性」，新會話能立即掌握真實的硬化邊界。
- 確立 4.6.21 為「真相對齊站」，為 4.8 真正的覺醒（移除 XP 門檻）掃除障礙。
